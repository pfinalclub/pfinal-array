# 更新日志

## 2.0.0 - 2026-09-22

### 破坏性变更

- 运行要求改为 PHP >= 8.0
- 移除 `PFarr` 单例和 `pf_*` 方法，公开接口改为 `pf\arr` 命名空间下的纯函数
- 数组变换返回新值，不再修改传入的数组

### 新功能

- 用 `pipe` / `compose` 把数组操作串成管道
- 补齐路径、序列、聚合、分组、树、集合、随机、编码和日期等常用函数
- 增加 `dot` / `undot`、`merge`、`orderBy`、`where`、`range`、`medianBy` 等操作

### 修复

- 修正去重时不同数组被当成同一个值的问题
- 修正 XML 文本中 `&`、`<` 丢失的问题
- 树结构出现环时停止继续下挂，避免无限递归
- 重命名撞键、空路径赋值、以及 `only` / `except` 额外参数不再静默丢数据

### 文档

- 按函数式接口重写 README 和使用示例
